{
  "name": "@sierra/route",
  "version": "0.0.0",
  "description": "Routing utilities for Client and Server",
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "scripts": {
    "start": "tsc",
    "test": "jest --verbose --coverage",
    "clean": "rimraf ./dist",
    "prepublishOnly": "rimraf ./dist && tsc --declaration",
    "watch": "tsc --watch --declaration"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "jest": "^26.6.3",
    "rimraf": "^3.0.2",
    "ts-jest": "26.5.4",
    "typescript": "^4.2.4"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": [
      "<rootDir>/dist/"
    ]
  }
}
